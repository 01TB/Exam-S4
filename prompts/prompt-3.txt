Crée les fonctions de CRUD de TypePret aussi.
Mets au points TypePret.php, TypePretController.php et type_pret_routes.php pour cela.
Fais usage de l'entité TypePret.

Voici la table TypePret : 
CREATE TABLE type_pret (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nom VARCHAR(255) NOT NULL,
    montant_max DECIMAL(15,2) NOT NULL,
    montant_min DECIMAL(15,2) NOT NULL,
    duree_remboursement_max INT NOT NULL,
    duree_remboursement_min INT NOT NULL,
    taux DECIMAL(5,2) NOT NULL
);
